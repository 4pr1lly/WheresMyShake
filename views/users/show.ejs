<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Users sign up page</title>
    </head>
    <body>
        <h1>Sign Up </h1>

        The <%=users.name%> is <%=users.username%>.
        <%if (users.password === true) { %>
            
        <% } else { %>
            incorrect password
        <% } %>
    </body>

</html>